# Copy to a real tfvars (e.g. prod.tfvars) and fill in. *.tfvars is gitignored,
# except this example file.
image           = "ghcr.io/joaovictorvm/focuzen:latest"
youtube_api_key = "your-youtube-api-key"
region          = "was"
